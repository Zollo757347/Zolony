type Query {
  login(username: String!, password: String!): LoginData
  getMap(username: String!, mapName: String!): GetMapData
}

type Mutation {
  createUser(username: String!, password: String!): CreateUserData
  editUser(data: EditUserInput!): UserData
  deleteUser(username: String!, password: String!): Boolean

  initialMyMap(data: initialMyMapInput!): MapData
  editMyMap(data: editMyMapInput!): MapData
  deleteUserMap(data: deleteUserMapInput!): Boolean
}

type UserData {
  username: String!
  avatar: String!
  bio: String!
  level: [Boolean!]!
  maps: [String!]!
}

type LoginData {
  error: String
  data: UserData
}

type GetMapData {
  error: String
  data: MapData
}

type CreateUserData {
  error: String
  data: UserData
}

input EditUserInput {
  name: String!
  password: String!
  newPassword: String
  newAvatar: String
  newBio: String
  newLevel: Int
}

input initialMyMapInput {
  name: String!
  password: String!
  mapName: String!
  xLen: Int!
  yLen: Int!
  zLen: Int!
}

input editMyMapInput {
  name: String!
  password: String!
  mapName: String!
  map: MapDataInput!
}

input deleteUserMapInput {
  name: String!
  password: String!
  mapName: String!
}

input MapDataInput {
  xLen: Int!
  yLen: Int!
  zLen: Int!
  mapName: String!
  availableBlocks: [Int!]
  validation: ValidationDataInput
  playground: [[[BlockDataInput]!]!]!
}

input ValidationDataInput {
  levers: [[Int!]!]
  lamps: [[Int!]!]
  boolFuncs: [[[Int!]!]!]
  timeout: Int!
}

input BlockDataInput {
  breakable: Boolean!
  type: Int!
  states: BlockStatesInput!
}

input BlockStatesInput {
  power: Int!
  source: Boolean!

  delay: Int
  facing: String
  face: String
  locked: Boolean
  powered: Boolean

  lit: Boolean

  east: Int
  south: Int
  west: Int
  north: Int
}

type MapData {
  xLen: Int!
  yLen: Int!
  zLen: Int!
  mapName: String!
  availableBlocks: [Int!]
  validation: ValidationData
  playground: [[[BlockData]!]!]!
}

type ValidationData {
  levers: [[Int!]!]
  lamps: [[Int!]!]
  boolFuncs: [[[Int!]!]!]
  timeout: Int
}

type BlockData {
  breakable: Boolean!
  type: Int!
  states: BlockStates!
}

type BlockStates {
  power: Int!
  source: Boolean!

  delay: Int
  facing: String
  face: String
  locked: Boolean
  powered: Boolean

  lit: Boolean

  east: Int
  south: Int
  west: Int
  north: Int
}